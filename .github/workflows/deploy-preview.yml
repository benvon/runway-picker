name: Deploy Preview

on:
  workflow_run:
    workflows: [CI]
    types: [completed]

permissions:
  actions: read
  contents: read
  pull-requests: write

jobs:
  resolve-preview:
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'pull_request'
    runs-on: ubuntu-latest
    outputs:
      eligible: ${{ steps.pull-request.outputs.eligible }}
      pull_number: ${{ steps.pull-request.outputs.number }}
      head_ref: ${{ steps.pull-request.outputs.headRef }}
      head_sha: ${{ steps.pull-request.outputs.headSha }}
    steps:
      - name: Resolve eligible pull request metadata
        id: pull-request
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3
        with:
          script: |
            const pullRequests = context.payload.workflow_run.pull_requests;
            if (pullRequests.length !== 1) {
              core.notice('Skipping preview: CI run is not associated with exactly one pull request.');
              core.setOutput('eligible', 'false');
              return;
            }

            const pullRequest = pullRequests[0];
            const { data } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pullRequest.number
            });

            if (
              data.base.ref !== 'main' ||
              data.head.repo?.fork ||
              ['dependabot[bot]', 'renovate[bot]'].includes(data.user.login) ||
              data.head.sha !== context.payload.workflow_run.head_sha
            ) {
              core.notice('Skipping preview: CI run is not an eligible same-repository pull request head.');
              core.setOutput('eligible', 'false');
              return;
            }

            core.setOutput('eligible', 'true');
            core.setOutput('number', String(data.number));
            core.setOutput('headRef', data.head.ref);
            core.setOutput('headSha', data.head.sha);

  deploy-preview:
    needs: resolve-preview
    if: needs.resolve-preview.outputs.eligible == 'true'
    runs-on: ubuntu-latest
    concurrency:
      group: preview-${{ needs.resolve-preview.outputs.pull_number }}
      cancel-in-progress: true
    # Configure this environment with required reviewers and a least-privilege
    # preview-only Cloudflare credential before enabling this workflow.
    environment: preview
    outputs:
      preview_url: ${{ steps.deploy.outputs.preview_deployment_url }}
      pull_number: ${{ needs.resolve-preview.outputs.pull_number }}
      head_sha: ${{ needs.resolve-preview.outputs.head_sha }}
    steps:
      - name: Checkout trusted deployment tooling
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 22
          cache: npm

      - name: Install trusted deployment tooling
        run: npm ci

      - name: Confirm the pull request head is still current
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3
        env:
          PULL_NUMBER: ${{ needs.resolve-preview.outputs.pull_number }}
          PR_HEAD_REF: ${{ needs.resolve-preview.outputs.head_ref }}
          PR_HEAD_SHA: ${{ needs.resolve-preview.outputs.head_sha }}
        with:
          script: |
            const pullNumber = Number.parseInt(process.env.PULL_NUMBER, 10);
            const { data } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pullNumber
            });

            if (
              data.state !== 'open' ||
              data.base.ref !== 'main' ||
              data.head.repo?.fork ||
              data.head.ref !== process.env.PR_HEAD_REF ||
              data.head.sha !== process.env.PR_HEAD_SHA
            ) {
              core.setFailed('Skipping preview deployment because the pull request head is no longer current.');
            }

      - name: Download CI-built preview artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: preview-dist
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ github.token }}
          path: dist

      - name: Validate preview artifact
        shell: bash
        run: |
          if [ ! -f dist/index.html ]; then
            echo 'Preview artifact is missing dist/index.html.'
            exit 1
          fi

          if find dist -type l -print -quit | grep -q .; then
            echo 'Preview artifact must not contain symbolic links.'
            exit 1
          fi

          if [ -e dist/_worker.js ]; then
            echo 'Preview artifact must not contain a Pages Worker script.'
            exit 1
          fi

      - name: Validate preview deployment configuration
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_PREVIEW_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_PROJECT_NAME: ${{ vars.CLOUDFLARE_PROJECT_NAME }}
        run: |
          if [ -z "$CLOUDFLARE_API_TOKEN" ] || [ -z "$CLOUDFLARE_ACCOUNT_ID" ] || [ -z "$CLOUDFLARE_PROJECT_NAME" ]; then
            echo 'Missing preview deployment configuration.'
            exit 1
          fi

      - name: Deploy preview to Cloudflare Pages
        id: deploy
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_PREVIEW_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_PROJECT_NAME: ${{ vars.CLOUDFLARE_PROJECT_NAME }}
          PR_HEAD_REF: ${{ needs.resolve-preview.outputs.head_ref }}
          PR_HEAD_SHA: ${{ needs.resolve-preview.outputs.head_sha }}
        run: |
          attempts=5
          for attempt in $(seq 1 "$attempts"); do
            set -o pipefail
            if npx wrangler pages deploy dist \
              --project-name "$CLOUDFLARE_PROJECT_NAME" \
              --branch "$PR_HEAD_REF" \
              --commit-hash "$PR_HEAD_SHA" \
              | tee deploy-output.txt; then
              break
            fi

            if [ "$attempt" -eq "$attempts" ]; then
              echo "Pages preview deploy failed after $attempts attempts."
              exit 1
            fi

            sleep_seconds=$((attempt * 10))
            echo "Retrying pages preview deploy in ${sleep_seconds}s..."
            sleep "$sleep_seconds"
          done

          preview_deployment_url="$(grep -Eo 'https://[^ ]+\.pages\.dev[^ ]*' deploy-output.txt | head -n1)"
          preview_branch_url="$(grep -Eo 'https://[^ ]+\.pages\.dev[^ ]*' deploy-output.txt | tail -n1)"
          if [ -z "$preview_deployment_url" ]; then
            echo 'Could not extract preview URL from wrangler output.'
            exit 1
          fi

          echo "preview_url=$preview_branch_url" >> "$GITHUB_OUTPUT"
          echo "preview_deployment_url=$preview_deployment_url" >> "$GITHUB_OUTPUT"

      - name: Smoke test preview cache behavior
        shell: bash
        env:
          PREVIEW_URL: ${{ steps.deploy.outputs.preview_deployment_url }}
        run: node scripts/verify-preview-cache.mjs "$PREVIEW_URL"

      - name: Post or update PR comment
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3
        env:
          PREVIEW_URL: ${{ steps.deploy.outputs.preview_url }}
          PULL_NUMBER: ${{ needs.resolve-preview.outputs.pull_number }}
        with:
          script: |
            const marker = '<!-- runway-picker-preview -->';
            const body = `${marker}\nPreview deployment ready: ${process.env.PREVIEW_URL}`;
            const issueNumber = Number.parseInt(process.env.PULL_NUMBER, 10);
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
            });

            const existing = comments.find((comment) =>
              comment.user?.type === 'Bot' && comment.body?.includes(marker)
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                body,
              });
            }

  verify-preview:
    needs: deploy-preview
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout pull request test suite without credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ needs.deploy-preview.outputs.head_sha }}
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 22
          cache: npm

      - name: Install pull request test dependencies
        run: npm ci

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Run preview UI E2E tests without deployment credentials
        shell: bash
        env:
          PREVIEW_URL: ${{ needs.deploy-preview.outputs.preview_url }}
        run: npm run test:e2e
